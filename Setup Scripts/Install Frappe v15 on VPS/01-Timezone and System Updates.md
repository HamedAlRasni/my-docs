# Timezone and System Updates

- Set the timezone to Asia/Aden
```shell
sudo timedatectl set-timezone "Asia/Aden"
```

- Update and upgrade the system
```shell
sudo apt update -y && apt upgrade -y
```
